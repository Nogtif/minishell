// on importe la librairie du shell.
#include "myshell.h"

/** Fonction principale qui s'occupe de lancer le programme.
 * @return : valeur de retour si tout c'est bien passé.
 */
int main(void) {
  // on appelle la méthode qui lance le shell.
  mysh();

  // on retourne la valeur par defaut.
  return 0;
}